<configuration>
    <!-- Include Spring Boot's default console appender and pattern -->
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <!-- Customize log level for your root package -->
    <logger name="de.unipassau.allocationsystem" level="DEBUG"/>
    <!-- Optional: Customize pattern or output, e.g., add file logging -->
    <!--
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/backend.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/backend.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>14</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
    -->

    <!-- Default root logger (console only, INFO level) -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>