# Test profile configuration
# This ensures DataInitializer with @Profile("!test") won't run during tests

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Flyway configuration for tests
spring.flyway.enabled=false
spring.flyway.clean-disabled=false
spring.flyway.baseline-on-migrate=true

# Email configuration - use same as production but EmailService is mocked in tests
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=noreplyallocationsystem1.com@gmail.com
spring.mail.password=zefm tyhh opwc snnk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.email.from=noreplyallocationsystem1.com@gmail.com

# JWT settings for tests
app.jwt.secret=test-secret-key-for-junit-tests-minimum-32-characters-required
app.jwt.expiration=86400000

# Logging
logging.level.de.unipassau.allocationsystem=DEBUG
